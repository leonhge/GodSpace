
import SwiftUI

struct TrackView: View {
    var body: some View {
        VStack {
            Text("Track Page")
                .font(.largeTitle)
                .padding()
            
            // Weitere Inhalte der Search-Seite hier hinzufügen
        }
    }
}

struct SearchView_Previews: PreviewProvider {
    static var previews: some View {
        TrackView()
    }
}
